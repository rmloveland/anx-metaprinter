AppNexus API "Metaprinter"
==========================

This command-line utility (installed as `metaprinter`) prints nicely
formatted wiki tables for AppNexus API docs.  It does this by fetching
the output of API service "meta" calls.

When provided with no arguments, it prints a help message like this:

    $ metaprinter
    Usage: metaprinter --{std,rpt} SERVICE

The `--std` flag means that you are trying to access a "standard" API
service, such as the [Publisher Service](), e.g.:

    $ metaprinter --std publisher

The `--rpt` flag means that you want a table printed for a reporting
API (one that lives under the [Reporting Service]()), e.g.:

    $ metaprinter --rpt network_analytics


INSTALLATION
------------

First, create the directory ~/bin/.metaprinter.

Then, create a file `auth` in that directory, which will hold your
JSON login credentials, e.g.,

    {
        "auth": {
            "password": "FOOBAR",
            "username": "rloveland"
        }
    }

After you save that file, change its permissions so no one else can read your password like so:

    $ chmod 600 auth


HACKING
-------

    ,config ,load /Users/rloveland/Code/mathoms/anx-metaprinter/interfaces.scm
    ,config ,load /Users/rloveland/Code/mathoms/anx-metaprinter/packages.scm
    ,open anx-metaprinter

	Load structure anx-metaprinter (y/n)? y
	[anx-api /Users/rloveland/Code/mathoms/anx-metaprinter/anx-api.scm]
	[anx-utils /Users/rloveland/Code/mathoms/anx-metaprinter/anx-utils.scm]
	[json-parser /Users/rloveland/Code/mathoms/anx-metaprinter/json-parser.scm]
	[vector-lib /Users/rloveland/Code/mathoms/anx-metaprinter/vector-lib.scm]
	[anx-docgen /Users/rloveland/Code/mathoms/anx-metaprinter/anx-docgen.scm]
	[anx-metaprinter /Users/rloveland/Code/mathoms/anx-metaprinter/main.scm]
